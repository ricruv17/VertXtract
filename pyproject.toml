[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "my_program"
version = "0.1.0"
description = "STM/AFM image processing"
requires-python = ">=3.14"

[project.scripts]
program_name = "vertxtract.cli:main"
